function Ej1()
{
  var angulo;
  var base;
  var altura;

  angulo=prompt("Inscribe la medida de un ángulo");
  angulo=parseInt(angulo);
  base=prompt("Inscribe la medida de la base");
  base=parseInt(base);
  altura=prompt("Inscribe la medida de la altura");
  altura=parseInt(altura);

  alert(base*altura/2)
}
